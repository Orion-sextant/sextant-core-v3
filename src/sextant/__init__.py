"""Sextant Core v3.0 — four-arm matched-budget algebra-ternary LM experiment.

The normative specification is PROTOCOL_v3.md. Reference kernels in reference/
are frozen; production code must agree with them bit-exactly where the protocol
says so (integer reference, signed-permutation kernel, rank catalogue).
"""

PROTOCOL_VERSION = "3.0"
